name: Trigger auto deployment for aits-react-api

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/aits-react-api-AutoDeployTrigger-95f3ff27-9be3-4b8b-a24e-80d9a5a09aee.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AITSREACTAPI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AITSREACTAPI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AITSREACTAPI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: aitsregistry.azurecr.io
          registryUsername: ${{ secrets.AITSREACTAPI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.AITSREACTAPI_REGISTRY_PASSWORD }}
          containerAppName: aits-react-api
          resourceGroup: AITS-Production
          imageToBuild: aitsregistry.azurecr.io/aits-react-api:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


